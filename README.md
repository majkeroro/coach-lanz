# Coach Lanz — Kru Lanz Fitness Gym 🥊

Live site: https://majkeroro.github.io/coach-lanz/

Muay Thai • Boxing • Strength & Conditioning — single-file website (`index.html`)
with real gym photos (`photos/`) and inline training reels (`videos/`).

## Edit
Open `index.html` in any editor. Photos live in `photos/`, videos in `videos/`.

## Deploy
Pushed to `main` branch. GitHub Pages serves from `/(root)`.
